/**
 * TypeScript type definitions for TradeAssist Frontend
 * 
 * Re-exports all types from models.ts for backward compatibility
 * and includes additional historical data types.
 */

// Export all model types (new camelCase versions)
export * from './models';

// Export historical data types
export * from './historicalData';